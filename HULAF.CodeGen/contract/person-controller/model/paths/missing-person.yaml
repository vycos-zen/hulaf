get:
  summary: Returns a missing person
  description: 
  responses:
    '200':
      description:
      content:
        application/json:
          schema: 
            $ref: '#/definitions/MissingPersonDto'