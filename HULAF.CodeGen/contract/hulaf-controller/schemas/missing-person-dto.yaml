MissingPersonDto:
  description: A missing person
  allOf:
    - $ref: '#/components/schemas/PersonDto'
    - type: object
      properties:
        lastSeenLocations:
          type: array
          items:
            $ref: '#/components/schemas/LocationDto'
